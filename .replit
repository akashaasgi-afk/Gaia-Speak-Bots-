# 1. Modules and Language Support
modules = ["python-3.12", "nodejs-22"]

[nix]
channel = "stable-25_05"
packages = ["inshellisense"]

# 2. Replit UI and Run Button Configuration
[workflows]
runButton = "Project"

[[workflows.workflow]]
name = "Start application"
author = "agent"

[workflows.workflow.metadata]
outputType = "webview"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "pip install flask gunicorn qdrant-client schedule && python3 patch_imports.py && gunicorn --bind=0.0.0.0:5000 --workers=1 main:app"
waitForPort = 5000

[[workflows.workflow]]
name = "Project"
mode = "parallel"
author = "agent"

[[workflows.workflow.tasks]]
task = "workflow.run"
args = "Start application"

[[ports]]
localPort = 5000
externalPort = 80

# 5. Global Deployment Settings
[deployment]
deploymentTarget = "autoscale"
run = "gunicorn --bind=0.0.0.0:5000 --workers=1 main:app"

[agent]
expertMode = true
